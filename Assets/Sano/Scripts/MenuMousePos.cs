using UnityEngine;
using System.Collections;

public class MenuMousePos : MonoBehaviour {

    public GameObject Fish;
    
    Vector3 mousePos;

	// Use this for initialization
	void Start () {
	    
	}
	
	// Update is called once per frame
	void Update () {
        //mousePos = Input.mousePosition;
        //Vector3 nowPos = new Vector3(mousePos.x/100, mousePos.y/100, 0);
        //Fish.transform.position = nowPos;
	}
}
